stages:
  - build

build-job:
  stage: build
  script:
    - echo "Compiling the code..."
    - cd src/cat
    - make s21_cat
    - cd ../grep
    - make s21_grep
    - echo "Compile complete."
  artifacts:
    paths:
      - src/cat/s21_cat
      - src/grep/s21_grep
    expire_in: 30 days