stages:
  - build

build-job:
  stage: build
  script:
    - echo "Compiling the code..."
    - cd src/cat
    - make s21_cat
    - cd ../grep
    - make s21_grep
    - echo "Compile complete."
  artifacts:
    paths:
      - src/cat/arti
      - src/grep/arti
    expire_in: 30 days

